"""
Tax-calculation engine for the taxcalc package.

This subpackage holds all of the computational code: the tax-calculating
functions, the Calculator that drives them, the parameter classes, the
Records/Data input classes, and the reporting utilities.  The data files
those classes read live in the sibling taxcalc.data subpackage.

Import the public API from the top-level package (``from taxcalc import
Calculator``) rather than from this subpackage directly.
"""
